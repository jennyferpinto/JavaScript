function squareNumber (Number1) {
	var squaredNum = Number1*Number1;
	console.log("The result of squaring the number " + Number1 + " is " + squaredNum)
	return squaredNum
}

squareNumber(2)

function halfNumber (Num) {
	var dividedNum = Num/2;
	console.log("Half of " + Num + " is " + dividedNum)
	return dividedNum
}

halfNumber(10)

function percentOf (Num1, Num2) {
	var percentNum = Num1/Num2;
	console.log(Num1 + " is " + percentNum + " of " + Num2)
	return percentNum
}

percentOf(50,100)

function areaofCircle (Radius) {
	var pi = 3.14;
	var areaForCircle = pi * Radius*Radius;
	console.log("The area for a circle with radius " + Radius + " is " + areaForCircle)
	return areaForCircle
}

areaofCircle(20)


function combinedOperations(Number) {
	
}